# 文字生视频模块 — 详细设计方案

## 一、现有能力复用清单

在动手写新代码之前，先盘点 pyvideotrans-studio 中**已经具备**的能力：

| 能力 | 位置 | 复用方式 |
|------|------|---------|
| TTS 配音 (Edge-TTS + 30+引擎) | `videotrans/tts/` | 直接调用，生成分镜旁白配音 |
| LLM 调用 (OpenAI 兼容) | `videotrans/translator/` | 复用 `_chatgpt.py` / `_deepseek.py` 的 API 调用模式 |
| SRT/ASS 字幕生成 | `videotrans/util/help_srt.py` | 根据 TTS 时间轴自动生成字幕 |
| FFmpeg GPU 编码 | `videotrans/util/help_ffmpeg.py` | 拼接素材 + 叠加字幕 + 混音 |
| GPU 检测 | `studio/editor/gpu_accel.py` | 启动时检测 NVENC/AMF/QSV |
| 视频预览 | `studio/editor/preview_widget.py` | 预览分镜素材片段 |
| 时间线 | `studio/editor/timeline_widget.py` | 展示分镜缩略图、拖拽排序、裁剪时长 |
| 导出对话框 | `studio/editor/export_dialog.py` | 横屏/竖屏切换、编码器选择 |
| 暗色主题 | `videotrans/styles/style.qss` | 统一加载 |
| 配置系统 | `videotrans/configure/config.py` | 扩展新配置项 |

**结论：约 60% 的核心能力已存在。** 需要新建的是文字→分镜脚本（LLM 分析）和素材获取层（本地搜索/Pexels/Pixabay/ComfyUI）。

---

## 二、架构设计

```
┌──────────────────────────────────────────────────────────────┐
│                  studio/main_window.py                       │
│  ┌─────────────┐ ┌──────────────────┐ ┌──────────────────┐  │
│  │ 文字输入面板  │ │   视频预览区      │ │  素材浏览器       │  │
│  │ (input)     │ │   (复用现有)      │ │  (media_browser) │  │
│  │ 分镜列表     │ │                  │ │  本地/在线/AI    │  │
│  └─────────────┘ └──────────────────┘ └──────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  时间线 = 分镜轨道 (复用 timeline_widget)                  │ │
│  │  [镜头1 3s] [镜头2 2s] [镜头3 4s] [镜头4 3s] [镜头5 3s]   │ │
│  └──────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│                    videotrans/text_to_video/                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────────┐  │
│  │ engine   │ │ LLM 分析  │ │ TTS 配音  │ │ 素材获取层      │  │
│  │ 管线编排  │ │ 分镜+提示词│ │ (复用)   │ │ local/pexels  │  │
│  │          │ │          │ │          │ │ /pixabay/comfy│  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ composer.py — FFmpeg 图片+音频+字幕 → 最终视频             │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**核心设计原则：** 复用现有时间线作为分镜轨道。每个分镜镜头 = 时间线上的一个 Clip。用户可以像编辑视频素材一样拖拽、排序、裁剪每个分镜的时长。

---

## 三、新增文件结构

```
studio/text_to_video/           # UI 层
├── __init__.py                 # 导出 TextToVideoPanel
├── input_panel.py              # 文字输入 + 素材源勾选 + 生成分镜按钮
├── storyboard_panel.py         # 分镜列表 (QListWidget 自定义渲染)
├── media_browser.py            # 素材浏览器 (本地搜索结果 + 在线预览图)
└── settings_dialog.py          # API Key / ComfyUI 地址 / TTS 选择

videotrans/text_to_video/       # 引擎层
├── __init__.py
├── engine.py                   # 管线编排器 (generate_pipeline)
├── llm_service.py              # LLM 分镜分析 (结构化 JSON 输出)
├── composer.py                 # FFmpeg 合成器 (图片序列 → 视频)
└── media/                      # 素材获取
    ├── __init__.py
    ├── base.py                 # 抽象基类 MaterialSource
    ├── local.py                # 本地文件扫描 + 关键词匹配
    ├── pexels.py               # Pexels API (免费注册)
    ├── pixabay.py              # Pixabay API (免费注册)
    └── comfyui.py              # ComfyUI 图片/视频生成
```

**总计：13 个新文件**，全部控制在合理大小（每个 200-500 行）。

---

## 四、数据模型

```python
# 分镜镜头 (对应时间线的一个 Clip)
@dataclass
class StoryboardShot:
    index: int                    # 镜头序号
    text: str                     # 镜头描述文字 ("阳光洒在金色沙滩上")
    duration: float               # 时长 (秒)
    keywords: list[str]           # 搜索关键词 ["阳光","沙滩","金色"]
    ai_prompt: str                # ComfyUI 图片提示词
    material_source: str          # "local" | "pexels" | "pixabay" | "comfyui" | "none"
    material_path: str            # 本地素材文件路径
    thumbnail: QPixmap | None     # 缩略图

# 项目配置
@dataclass 
class TextToVideoConfig:
    input_text: str               # 用户输入的文字
    shots: list[StoryboardShot]   # 分镜列表
    # 素材源
    use_local: bool = True
    use_pexels: bool = False
    use_pixabay: bool = False
    use_comfyui: bool = False
    # TTS
    tts_voice: str = "zh-CN-YunjianNeural"
    tts_speed: float = 1.2
    # 输出
    orientation: str = "landscape"  # portrait / landscape
    resolution: tuple = (1920, 1080)
    fps: int = 30
    bgm_enabled: bool = False
    subtitle_enabled: bool = True
```

---

## 五、完整工作流（管线编排）

```
用户输入 "夏日海边的美好时光"
│
▼
① engine.generate_storyboard(text)
   └→ LLM 分析 → 结构化分镜 JSON
       {
         "shots": [
           {"text": "阳光洒在金色沙滩上", "duration": 3, "keywords": ["阳光","沙滩","金色"],
            "ai_prompt": "A golden sandy beach under bright sunlight, 4K, cinematic"},
           ...
         ]
       }
│
▼
② 用户确认分镜 (可编辑每个镜头的文字/时长/关键词/提示词)
│
▼
③ engine.acquire_materials(shots)
   对每个镜头，按优先级获取素材:
   ├→ local.py:    扫描用户指定目录，关键词匹配文件名
   ├→ pexels.py:   API 搜索 → 下载预览 → 用户选择 → 下载高清
   ├→ pixabay.py:  API 搜索 → 下载预览 → 用户选择 → 下载高清
   └→ comfyui.py:  提交 workflow → 等待生成 → 下载结果
│
▼
④ engine.generate_tts(text, voice, speed)
   └→ Edge-TTS → narrator.mp3 + .srt 字幕文件
│
▼
⑤ engine.compose_video(shots, audio, subtitle, config)
   └→ FFmpeg:
       1. 每个无视频的镜头: 图片 + 音频片段 → 带淡入淡出的短视频段
       2. 拼接所有视频段
       3. 叠加字幕
       4. 混合 BGM (可选)
       5. GPU 编码输出
│
▼
⑥ 输出最终视频 → 可在预览区播放 → 用户满意后保存
```

---

## 六、各模块详细设计

### 6.1 LLM 分镜分析 (`llm_service.py`)

```
输入: 用户文字 (如 "夏日海边的美好时光")
输出: 结构化分镜 JSON

核心要点:
- 使用现有的 OpenAI 兼容 API (复用 videotrans/translator/_chatgpt.py 模式)
- System prompt 硬编码为中文分镜脚本生成器
- 要求 LLM 返回严格的 JSON 格式，包含 shots 数组
- 每个 shot: {text, duration, keywords[], ai_prompt}
- 加入 JSON 解析容错 (尝试修复常见格式错误)
- 提供默认分镜回退 (LLM 不可用时，按句号/换行简单拆分)
```

### 6.2 素材获取层 (`media/`)

```
优先级策略 (可配置):
  ① 本地匹配结果 (最快，无网络)
  ② Pexels/Pixabay 搜索结果 (免费，海量)
  ③ ComfyUI AI 生成 (最慢，最定制化)

base.py:
  - 抽象类 MaterialSource
  - 接口: search(keywords, count) → list[MaterialResult]
  - 接口: download(material) → local_path

local.py:
  - 用户指定 1-N 个本地素材目录
  - 递归扫描视频/图片文件
  - 用关键词与文件名做模糊匹配 (difflib.SequenceMatcher)
  - 返回匹配度排序的素材列表

pexels.py / pixabay.py:
  - 调用免费 REST API
  - 先获取预览图列表 (小缩略图)
  - 用户确认后再下载原尺寸
  - 做好 rate limit 处理和缓存

comfyui.py:
  - 连接用户本地 ComfyUI 服务 (默认 http://127.0.0.1:8188)
  - 加载内置 workflow JSON 模板 (txt2img.json)
  - 替换 prompt 节点中的文本
  - 提交 → 轮询状态 → 下载生成结果
  - 无 GPU 时自动灰度禁用此选项
```

### 6.3 FFmpeg 合成器 (`composer.py`)

```
输入: 分镜列表 (含素材路径) + 旁白音频 + 字幕文件 + 输出配置
输出: 完整视频文件

合成步骤:
1. 对每个分镜镜头:
   a. 如果素材是图片:
      - ffmpeg -loop 1 -i image.jpg -i audio_segment.wav \
          -vf "scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2,fade=t=in:d=0.5,fade=t=out:d=0.5" \
          -c:v h264_nvenc -t <duration> segment_N.mp4
   b. 如果素材是视频:
      - 直接裁剪到指定时长 + 淡入淡出
2. 用 concat demuxer 拼接所有分段
3. 叠加 ASS/SRT 字幕
4. 混合 BGM (可选，音量 20%)
5. GPU 硬件编码输出

关键策略:
- 自动检测 GPU 编码器 (复用现有 gpu_accel.py)
- 生成过程实时输出进度百分比
- 失败自动回退 CPU 编码
```

### 6.4 管线编排器 (`engine.py`)

```
class TextToVideoEngine(QObject):
    # 信号
    progress = Signal(str, int)        # 步骤描述, 百分比
    shot_ready = Signal(int, QPixmap)  # 某个分镜素材就绪
    finished = Signal(str)             # 输出视频路径
    error = Signal(str)                # 错误信息

    # 核心方法
    def generate_storyboard(text: str) → list[StoryboardShot]
    def acquire_materials(shots: list, sources: list[MaterialSource])
    def generate_tts(text: str, voice: str) → (audio_path, srt_path)
    def compose(shots, audio, srt, config) → output_path
    def run_full_pipeline(config: TextToVideoConfig)  # 一键全流程

    # 每个步骤跑在 QThread 中，通过 Signal 通知 UI
```

### 6.5 UI 面板设计

```
┌─────────────────────┬───────────────────────────┬──────────────────────┐
│  文字输入面板        │                           │  素材浏览器            │
│  ┌────────────────┐ │      视频预览区             │  ┌────────────────┐  │
│  │ 多行文字输入    │ │      (复用 preview_widget)  │  │ [本地] [在线]   │  │
│  │                │ │                           │  │ [AI生成]       │  │
│  │                │ │                           │  │                │  │
│  └────────────────┘ │                           │  │ [图] [图] [图] │  │
│                     │                           │  │ [图] [图] [图] │  │
│  素材来源            │                           │  │                │  │
│  ☑ 本地素材         │                           │  │ 拖到时间线替换   │  │
│  ☑ 在线搜素         │                           │  └────────────────┘  │
│  ☐ AI 生成 (ComfyUI)│                           │                      │
│                     │                           │                      │
│  分镜列表            │                           │                      │
│  ┌────────────────┐ │                           │                      │
│  │① 阳光沙滩 [3s]  │ │                           │                      │
│  │② 海浪礁石 [2s]  │ │                           │                      │
│  │③ 海边嬉戏 [4s]  │ │                           │                      │
│  │④ 夕阳海岸 [3s]  │ │                           │                      │
│  │⑤ 夜空篝火 [3s]  │ │                           │                      │
│  └────────────────┘ │                           │                      │
├─────────────────────┴───────────────────────────┴──────────────────────┤
│  时间线 (复用 timeline_widget)                                          │
│  [海滩图 3s] [海浪图 2s] [嬉戏图 4s] [夕阳图 3s] [篝火图 3s]            │
├────────────────────────────────────────────────────────────────────────┤
│  [生成分镜] [搜索素材] [AI生成] [生成配音] [合成导出]      进度: ████░░ │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 七、实施路线图

### 阶段 1：核心骨架（预计 4-6 个文件）
- `studio/text_to_video/__init__.py`
- `studio/text_to_video/input_panel.py` — 文字输入 + 素材源勾选
- `videotrans/text_to_video/__init__.py`
- `videotrans/text_to_video/llm_service.py` — LLM 分镜分析
- `videotrans/text_to_video/engine.py` — 管线编排骨架
- 在 `main_window.py` 添加入口按钮

**验收标准：** 输入文字 → LLM 返回分镜 JSON → 打印到控制台

### 阶段 2：素材获取（预计 5 个文件）
- `videotrans/text_to_video/media/base.py`
- `videotrans/text_to_video/media/local.py`
- `videotrans/text_to_video/media/pexels.py`
- `videotrans/text_to_video/media/pixabay.py`
- `studio/text_to_video/media_browser.py`

**验收标准：** 本地搜索返回匹配图片、Pexels 搜索返回预览图列表

### 阶段 3：合成与导出（预计 2 个文件）
- `videotrans/text_to_video/composer.py` — FFmpeg 合成器
- `studio/text_to_video/storyboard_panel.py` — 分镜可视化

**验收标准：** 选几张图片 + 一段 TTS 音频 → 输出完整视频

### 阶段 4：整合与优化（预计 2 个文件）
- `studio/text_to_video/settings_dialog.py` — 配置面板
- ComfyUI 集成 (`videotrans/text_to_video/media/comfyui.py`)

**验收标准：** 全流程跑通，GUI 中完成从文字到视频的全部操作

---

## 八、关键技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 分镜展示方式 | **复用现有 timeline_widget** | 避免重复造轮子，分镜=Clip 语义天然匹配 |
| LLM 接口 | **复用现有 OpenAI 兼容 API** | 用户已配置好 API Key，零额外成本 |
| 素材源架构 | **抽象基类 + 插件式** | 方便后续加 Unsplash、Google Image 等新源 |
| ComfyUI | **阶段 4 再做** | 需要 GPU，不是所有用户都有；先保证基本流程可跑 |
| 项目文件格式 | **扩展现有 v5 格式** | 加 `text_to_video` 字段，不破坏现有保存/加载 |
| 图片→视频 | **FFmpeg loop + fade** | 无需引入新依赖，效果足够好 |
