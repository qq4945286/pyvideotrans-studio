# pyvideotrans-studio 操作使用说明

## 界面概览

启动软件后，主窗口分为以下几个区域：

```
┌─────────────────────────────────────────────────────────────┐
│  菜单栏 / 工具栏                                            │
├──────────────────────────────┬──────────────────────────────┤
│                              │                              │
│   视频预览区域                │   翻译配音参数面板            │
│   （播放/暂停/逐帧）          │   （源语言/目标语言/配音）    │
│                              │                              │
├──────────────────────────────┴──────────────────────────────┤
│  时间线区域                                                  │
│  （素材轨道 / 裁剪 / 字幕编辑）                                │
├──────────────────────────────────────────────────────────────┤
│  状态栏 / 进度                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 基础功能

### 导入视频素材

1. **从文件管理器拖拽**视频文件到窗口任意区域
2. 或点击 **"打开视频"** 按钮选择文件
3. 支持格式：MP4, AVI, MKV, MOV, FLV, WebM

导入后视频会自动加载到预览区，时间线显示缩略图轨道。

### 视频预览

预览区提供以下控制：

| 按钮 | 快捷键 | 功能 |
|------|--------|------|
| ▶ / ⏸ | **空格键** | 播放 / 暂停 |
| ⏮ | ← | 上一帧 |
| ⏭ | → | 下一帧 |
| ⏪ | Ctrl+← | 快退 5 秒 |
| ⏩ | Ctrl+→ | 快进 5 秒 |
| 🔇 | M | 静音切换 |
| 时间输入框 | 直接输入 | 跳转到指定时间（格式：秒数或 mm:ss） |

时间输入框支持即时跳转，输入数字后自动定位到对应时间位置。

### 时间线操作

时间线区域用于管理视频片段和字幕：

- **缩放滑块**：左右拖动调整时间线显示精度
- **裁剪**：拖动时间线两端手柄裁剪视频起止点
- **滚动**：鼠标滚轮或拖动滚动条浏览全部时间线

---

## 翻译 / 配音功能

这是本软件的核心功能 — 将视频中的语音翻译并配音为目标语言。

### 基本流程

```
导入视频 → 设置翻译参数 → 执行翻译配音 → 导出结果
```

### 翻译配音参数面板

位于主窗口右侧，需设置以下参数：

#### ① 源语言和目标语言

| 参数 | 说明 |
|------|------|
| **源语言** | 视频原声的语言（如 中文、英文） |
| **目标语言** | 要翻译成的语言（如 英文 → 中文） |
| **源语种配音** | 保留原语言配音（与原视频配音不同角色） |

> 源语言会在识别时由系统自动判断，也可手动指定。

#### ② 语音识别（ASR）

| 选项 | 说明 |
|------|------|
| **识别模型** | 选择语音识别引擎 |
| 本地模型 | Faster-Whisper, FunASR 等（无需网络，需下载模型） |
| 云端接口 | 百度、阿里、腾讯、字节等 API 识别服务 |
| **语言检测** | 自动检测语言或手动指定 |
| **VAD 分割** | 语音活动检测，自动分割静音段落 |

#### ③ 翻译引擎

| 引擎类型 | 支持的服务 |
|---------|-----------|
| AI 大模型 | ChatGPT, DeepSeek, Gemini, 通义千问, Claude, 豆包 |
| 在线翻译 | Google 翻译, DeepL, 百度翻译, 腾讯翻译 |
| 本地模型 | 可接入本地 LLM |

#### ④ 配音（TTS）

| 选项 | 说明 |
|------|------|
| **配音角色** | 选择目标语言的发音人 |
| **配音语速** | -50% ~ +50% |
| **配音音量** | -95% ~ +100% |
| **配音音调** | -100Hz ~ +100Hz |
| **多角色配音** | 为不同说话人分配不同声音 |

#### ⑤ 其他设置

| 参数 | 位置 | 说明 |
|------|------|------|
| 配音高级设置 | 工具栏 → **配音高级设置参数** | VAD、并发、背景音、字幕样式等 |

---

### 执行翻译配音

1. 设置好上述参数后，点击 **"开始执行"** 按钮
2. 进度栏显示当前处理步骤和处理进度：
   ```
   ████████░░░░ 准备音频...
   ████████████ 语音识别中...
   ████████████ 翻译字幕...
   ████████████ 配音合成...
   ████████████ 视频合成...
   ```
3. 处理完成后自动弹出导出预览

### 字幕编辑

翻译完成后，字幕会显示在下方面板中：

- **双击字幕行**可编辑文本内容
- **修改后自动保存**，重新播放时生效
- 支持中英文双语字幕显示

---

## 高级设置说明

点击顶部工具栏的 **"配音高级设置参数"** 打开详细参数对话框。

### VAD 语音活动检测

控制语音识别的断句精度：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| threshold | 0.5 | 检测灵敏度（越小越灵敏） |
| min_speech_duration_ms | 1000 | 最短语音段（毫秒） |
| min_silence_duration_ms | 250 | 最小静音长度（毫秒） |
| max_speech_duration_s | 8 | 最长语音段（秒） |
| 标点恢复 | 关 | 自动添加标点符号 |
| 说话人分类 | 关 | 区分不同说话人（需指定人数） |

### 翻译并发参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 普通每批行数 | 5 | 普通翻译每批处理字幕行数 |
| AI 每批行数 | 100 | AI 翻译每批处理行数 |
| 等待时间 | 0s | 请求间隔（防限流，长文本建议设置） |

### 配音微调

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 配音等待 | 0s | 每句配音前延迟 |
| 语速 | 0% | 配音播放速度 |
| 音量 | 0% | 配音音量增益 |
| 音调 | 0Hz | 配音音调偏移 |

### 硬字幕排版

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 中日韩单行字符 | 20 | 每行最大中/日/韩文字数 |
| 其他语言 | 60 | 每行最大英/法/俄等字符数 |
| 修改硬字幕样式 | — | 打开字体、颜色、边框等详细设置 |

### 背景音管理

| 参数 | 说明 |
|------|------|
| 人声背景音分离 | 是否分离原视频人声和背景音 |
| 嵌入背景音乐 | 为新配音添加背景音乐 |
| BGM 处理方式 | 短暂拉长 / 循环播放 |
| 背景音音量 | 0.0 ~ 1.0 |
| 选择背景音 | 选择本地音频文件作为背景音 |

---

## 视频剪辑功能

软件提供基础剪辑能力：

### 裁剪视频

1. 在时间线上拖动裁剪手柄
2. 点击 **"裁剪"** 应用
3. 裁剪区域之外的视频将被去除

### 调整音量

1. 选中时间线上的素材
2. 调节音量滑块（0% ~ 200%）

### 导出视频

1. 完成翻译配音后，点击 **"导出"**
2. 选择输出格式和品质
3. 支持：原始画质导出 / 压缩导出

---

## 命令行选项

```bash
python main.py              # 正常启动
```

无其他命令行参数，所有功能在图形界面中完成。

---

## 提示与技巧

### 性能优化

- **GPU 加速**：NVIDIA 用户安装 CUDA 版 torch，AMD 用户安装 ROCm 版
- **内存**：处理长视频时确保可用内存充足
- **临时文件**：处理过程中会占用磁盘空间，处理完成后自动清理

### 提高识别准确率

1. 原视频尽量清晰、无背景噪音
2. 说话人口音标准
3. 适当调整 VAD 参数

### 提高翻译质量

1. 使用 AI 大模型（ChatGPT、DeepSeek 等）翻译效果最佳
2. 在引擎设置中填写 API Key 和地址
3. 可自定义翻译提示词（prompt）

---

## 常见操作问题

### 处理到一半卡住了

1. 检查终端窗口是否有错误输出
2. 可能原因：网络超时（使用云端服务）、内存不足
3. 可重新开始执行

### 字幕时间不同步

- 检查 VAD 参数是否合适
- 说话人语速较快时，可调小 `max_speech_duration_s`
- 说话人停顿较长时，可调大 `min_silence_duration_ms`

### 配音角色没有声音

- 确认所选配音引擎 API 可用
- 部分角色需要网络下载语音模型（首次较慢）
- 可在对应引擎设置页面测试连接

### 导出视频没有字幕

- 确认勾选了"嵌入字幕"选项
- 硬字幕需要渲染，耗时较长，请等待完成

---

## 技术支持

如遇问题，可在终端运行环境检测查看完整诊断信息：

```bash
python -c "from studio.setup_wizard import run; run(force=True)"
```

报告问题时请附上终端输出和 `tmp/` 目录下的日志文件。
